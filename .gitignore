
# Azure Functions artifacts
bin
obj
appsettings.json
local.settings.json

# Local and secret-bearing configuration
# The package pipeline_config.json is a tracked, sanitised production-default file.
# Credentials must remain external via environment variables.
/classification_rules.json
/pipeline_config.json
packages/pipeline/classification_rules.json
packages/local-cockpit-server/.env.local


# Azurite artifacts
__blobstorage__
__queuestorage__
__azurite_db*__.json

# Local Exports/Logs
TranscriptExport/
*.log
transcript_log_*.*
AzuriteConfig

# Node.js / Wrangler (future Worker packages)
node_modules/
.wrangler/
dist/
.dev.vars
*.local.vars

# Local-only controlled fixture exercises (never commit)
.local-fixtures/
