{
  "name": "@eip/cloudflare-runtime",
  "version": "0.1.0",
  "description": "Cloudflare runtime for raw transcript processing and topic memory persistence.",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260721.1",
    "miniflare": "^5.20260801.1-alpha",
    "typescript": "^5.8.3",
    "vitest": "^3.2.7",
    "wrangler": "^4.118.0"
  }
}
